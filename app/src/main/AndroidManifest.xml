<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="VLTV PLAY"
        android:supportsRtl="true"
        android:theme="@style/Theme.VLTVPlay"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- SUAS ACTIVITIES (IGUAIS) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/Theme.VLTVPlay">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".HomeActivity" android:screenOrientation="sensorLandscape" android:theme="@style/Theme.VLTVPlay" />
        <activity android:name=".LiveTvActivity" android:screenOrientation="sensorLandscape" android:theme="@style/Theme.VLTVPlay" />
        <activity android:name=".PlayerActivity" android:screenOrientation="sensorLandscape" android:theme="@style/Theme.VLTVPlay" />
        <activity android:name=".VodActivity" android:screenOrientation="sensorLandscape" android:theme="@style/Theme.VLTVPlay" />
        <activity android:name=".DetailsActivity" android:screenOrientation="sensorLandscape" android:theme="@style/Theme.VLTVPlay" />
        <activity android:name=".SeriesActivity" android:screenOrientation="sensorLandscape" android:theme="@style/Theme.VLTVPlay" />
        <activity android:name=".SeriesDetailsActivity" android:screenOrientation="sensorLandscape" android:theme="@style/Theme.VLTVPlay" />
        <activity android:name=".SearchActivity" android:screenOrientation="sensorLandscape" android:theme="@style/Theme.VLTVPlay" />
        <activity android:name=".SettingsActivity" android:screenOrientation="sensorLandscape" android:theme="@style/Theme.VLTVPlay" />
        <activity android:name=".DownloadsActivity" android:screenOrientation="sensorLandscape" android:theme="@style/Theme.VLTVPlay" />

        <!-- ✅ RECEIVER CORRIGIDO -->
        <receiver 
            android:name=".AppUpdateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
